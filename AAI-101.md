# AAI-101: Core Protocol Specification

## Purpose

AAI-101 defines the core message protocol between Android devices and embedded controllers.

## Initial Transport

JSON-lines over serial.

## Required Flow

`HELLO` → `CONFIG` → `ARM_REQUEST` → `ARM_OK` → `COMMAND` / `ACK`

Commands outside the advertised safety limits must return `ERROR`.
